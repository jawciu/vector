// Menu moved into the DS layer: app/ui/Menu.tsx (DS-PLAN Phase 5, step 4).
// This shim keeps every existing `components/Menu` import working unchanged —
// import from "@/app/ui/Menu" in new code.
export { MenuTriggerButton, MenuList, MenuOption } from "@/app/ui/Menu";
